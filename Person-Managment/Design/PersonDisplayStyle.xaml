<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Person_Managment.Design">
    <!--
    Stil grafičnih komponent v gradniku PersonDisplay
    -->
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="./ColorPalette.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="Grid" x:Key="container">
        <Setter Property="Margin" Value="15"/>
    </Style>
    <Style TargetType="Label" x:Key="label_keywords">
        <Setter Property="Foreground" Value="{StaticResource color_dark_text}"/>
    </Style>
    <Style TargetType="TextBox" x:Key="label_data">
        <Setter Property="Foreground" Value="{StaticResource color_medium_text}"/>
        <Setter Property="BorderBrush" Value="{StaticResource color_medium_text}"/> 
        <Setter Property="VerticalContentAlignment" Value="Center"/> 
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style TargetType="Label" x:Key="data_display_header">
        <Setter Property="Foreground" Value="{StaticResource color_dark_text}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Margin" Value="0,0,0,5"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
</ResourceDictionary>